name: Deploy to GKE using Docker Hub

on:
  push:
    branches:
      - main

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GKE_CLUSTER: ${{ secrets.GKE_CLUSTER }}
  GKE_ZONE: ${{ secrets.GKE_ZONE }}
  IMAGE_NAME: flask-app

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and Push Docker image
      uses: docker/build-push-action@v5
      with:
        push: true
        tags: ${{ secrets.DOCKERHUB_USERNAME }}/flask-app:latest

    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCP_SA_KEY }}

    - name: Connect to Bastion and Deploy
      uses: appleboy/ssh-action@v1.1.0
      with:
        host: ${{ secrets.BASTION_HOST }}
        username: ${{ secrets.BASTION_USER }}
        key: ${{ secrets.BASTION_SSH_KEY }}
        script: |
          echo "Authenticating to GKE..."
          sudo apt-get install google-cloud-cli-gke-gcloud-auth-plugin
          gcloud container clusters get-credentials mariam-gke --zone us-central1-a --project my-fun-playground 
          echo "Haha"
#          gcloud compute ssh --zone "us-central1-a" "private-bastion" --tunnel-through-iap --project "my-fun-playground"
          
 #         apt-get install google-cloud-cli-gke-gcloud-auth-plugin
#          gcloud container clusters get-credentials ${{ secrets.GKE_CLUSTER }} --zone ${{ secrets.GKE_ZONE }} --project ${{ secrets.GCP_PROJECT_ID }}

  #        echo "Applying manifests..."
 #         kubectl apply -f /path/to/.k8s/deployment.yaml
#          kubectl apply -f /path/to/.k8s/service.yaml
#          kubectl rollout status deployment/flask-app

